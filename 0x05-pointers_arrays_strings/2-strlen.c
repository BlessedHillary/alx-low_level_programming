#include <stdio.h>

/**
 * _strlen - Returns cjjd skks
 * @str: The ste djdd ss
 * Return: the djdjm
 */

size_t _strlen(const char *str)

{
	size_t length = 0;

	while (*str++)
	length++;
	return (length);
}
